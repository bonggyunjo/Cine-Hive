package com.example.CineHive.repository;

import com.example.CineHive.entity.PMovie;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PMovieRepository extends JpaRepository<PMovie, Long>{
}
